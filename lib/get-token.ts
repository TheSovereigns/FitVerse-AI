/**
 * @deprecated Import from "@/lib/auth/getToken" instead.
 * Shim kept for backward compatibility — re-exports getToken / getAuthToken.
 */
export { getToken, getAuthToken } from "./auth/getToken"
